<Window x:Class="Quick.SelectParamsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quick"
        mc:Ignorable="d"
        Title="Quick" Height="600" Width="400" ResizeMode="NoResize" Closed="Window_Closed" >
    <Grid x:Name="mainGrid" Background="DarkGray">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap" TextAlignment="Center" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="20">
            Выберите параметры и свойства, 
            которые должны быть одинаковы у выбранного объекта. 
            и объектов поиска</TextBlock>
        <ListBox x:Name="CheckBoxList" Grid.Row="1">
        </ListBox>
        <Button Grid.Row="3" Click="SelectSameTypeExtended">
            <TextBlock TextWrapping="Wrap" TextAlignment="Center" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="50">
            Начать поиск
            </TextBlock>
        </Button>
    </Grid>
</Window>
